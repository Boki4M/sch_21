#include <stdio.h>

void graf_f(int x_f, int y_f);  // Объявление функции для отображения позиций символов

int main() {
    int x_full, y_full;  // Объявление переменных для хранения позиций символов

    printf("Enter the \"X\" and \"O\" position:");  // Вывод приглашения для ввода данных

    // Проверка корректности введенных данных:
    // 1. Успешно ли считаны два числа
    // 2. Не являются ли позиции отрицательными числами
    if (scanf("%d%d", &x_full, &y_full) != 2 || x_full < 0 || y_full < 0) {
        printf("Invalid position");  // Вывод сообщения об ошибке при невалидных данных
        return -1;                   // Возврат кода ошибки из программы
    }

    graf_f(x_full, y_full);  // Вызов функции отображения символов с передачей позиций
}



    // Определение максимальной позиции для определения длины выводимой строки
    int max_pos = (x_f > y_f) ? x_f : y_f;

    // Цикл для последовательного вывода символов от позиции 0 до максимальной
    for (int i = 0; i <= max_pos; i++) {
        // Если текущая позиция соответствует позиции крестика
        if (i == x_f) {
            // Вывод символа крестика
            printf("%c", x);
        }
        // Если текущая позиция соответствует позиции нолика
        else if (i == y_f) {
            // Вывод символа нолика
            printf("%c", y);
        }
        // Для всех остальных позиций
        else {
            // Вывод пробела
            printf("%c", space);
        }
    }
}